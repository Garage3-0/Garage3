
@* @model IEnumerable<ParkedVehicleOverviewViewModel> *@

@{
    ViewData["Title"] = "Garage 3"; /* Parked Vehicles Overview */
    // string currentSearch = ViewData["CurrentFilter"]?.ToString() ?? "";
}
<div class="/*d-flex justify-content-between align-items-center*/">
  <h1>Welcomme to Garage 3</h1>
    <div style="display: grid; row-gap: 20px; margin-top: 30px;">
       
        <div style="display: flex; align-items: center; column-gap: 12px;">
            <a class="btn btn-sm bg-primary text-white" asp-area="Identity" asp-page="/Account/Login">Login</a>
            Login to handle your vehicles and parkings
        </div>

        <div style="display: flex; align-items: center; column-gap: 12px;">
            <a class="btn btn-sm bg-success text-white" asp-area="Identity" asp-page="/Account/Register">Register</a>
            Wants to become a member
        </div>
  </div>
</div>